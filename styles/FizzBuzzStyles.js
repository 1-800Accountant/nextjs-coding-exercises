import styled from 'styled-components';

const FizzBuzzStyles = styled.div`
  margin: 0 auto;
  max-width: 1140px;

  .header {
    align-items: center;
    display: flex;
    justify-content: space-between;
  }

  .problem-container {
    .directions {
      border-bottom: 1px solid grey;
    }
  }
`;

export default FizzBuzzStyles;